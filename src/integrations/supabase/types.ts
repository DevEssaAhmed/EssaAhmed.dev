export type Json = any;

// Minimal typing to keep Supabase usage flexible during migration.
// Replace with generated types from Supabase when available.
export type Database = any;
export type Tables<_TableName extends string = string> = any;
